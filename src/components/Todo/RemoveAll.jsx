import { Button } from "antd";
import React from "react";

const RemoveAll = () => {
  return (
    <Button
      type="primary"
      style={{
        background: "#5ba3c9",
      }}
    >
      Remove checked
    </Button>
  );
};

export default RemoveAll;
